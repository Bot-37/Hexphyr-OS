[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = "0.26.0"
uefi-services = "0.26.0"
log = "0.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
codegen-units = 1