[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.26.0", features = ["alloc", "global_allocator"] }
bootabi = { path = "../bootabi" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
codegen-units = 1
